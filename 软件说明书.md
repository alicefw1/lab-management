# 实验室综合管理系统 · 软件说明书

## 1. 软件基本信息

| 项目 | 内容 |
| --- | --- |
| 软件名称 | 实验室综合管理系统 |
| 软件版本 | V1.0 |
| 开发语言 | Java、JavaScript、HTML、CSS、SQL |
| 软件类型 | 应用软件（Web 应用） |
| 运行环境 | Windows / Linux，JDK 8+，MySQL 8 |
| 开发工具 | IntelliJ IDEA、Maven |

## 2. 开发目的

传统实验室设备管理依赖人工登记与纸质台账，存在信息不透明、审批流程繁琐、设备利用率难以统计等问题。本系统旨在通过信息化手段，实现实验室设备从入库建档、借用审批、归还流转、报修维护到数据统计的全生命周期数字化管理，提升管理效率与规范化水平，为高校实验室的设备资产提供统一的在线管理平台。

## 3. 软件主要功能

系统采用前后端分离架构，按角色提供三套工作台，覆盖以下核心业务：

### 3.1 用户与权限管理
- 支持账号注册与登录，采用 JWT 无状态鉴权；
- 基于注解（`@RequireRole`）实现角色权限控制；
- 支持 5 种角色：学生、教师、实验员、管理员、系主任；
- 管理员可对用户进行增删改查、角色分配、冻结/解冻、密码重置。

### 3.2 实验室信息管理
- 实验室信息建档（名称、位置、容量、负责人、描述）；
- 实验室增删改查与状态维护。

### 3.3 实验室与设备预约
- 实验室预约（日期、时间段、用途），时段冲突校验；
- 设备预约（日期、时间段、用途），时段冲突校验；
- 实验员/管理员审核预约（通过/拒绝/取消）。

### 3.4 设备台账管理
- 设备信息建档（编号、名称、分类、型号、位置、采购日期、状态、备注）；
- 设备分类维护、设备检索；
- 设备状态流转（可借 → 已借出 → 维修中 → 可借 → 报废）。

### 3.5 借用申请与审批、归还与逾期管理
- 学生/教师在线提交借用申请（含诚信分校验）；
- 实验员/管理员审批，通过后自动生成借用记录并更新设备状态；
- 归还登记，逾期自动识别与刷新；
- 设备损失登记，损失后扣除诚信分并报废设备。

### 3.6 报修维护管理
- 学生/教师提交报修工单（故障描述）；
- 实验员/管理员处理工单（维修中/已完成），处理结果留痕。

### 3.7 设备采购管理
- 用户提交采购申请（设备名称、型号、数量、单价、供应商、用途）；
- 管理员审批（批准/拒绝/已采购）。

### 3.8 实验室耗材管理
- 耗材信息建档（名称、规格、单位、数量、分类）；
- 耗材增删改查与库存维护。

### 3.9 危化品标准库
- 危化品信息维护（CAS号、名称、危险等级、类别、单位、描述）；
- 危化品增删改查。

### 3.10 废弃物管理
- 废弃物登记（类型、来源、数量、单位）；
- 废弃物审核验收（通过/驳回）；
- 废弃物处置。

### 3.11 诚信评分体系
- 初始诚信分 100 分；
- 逾期归还扣 10 分，设备损失扣 30 分；
- 诚信分低于 60 分禁止借用设备；
- 管理员可手动调整分数，变更记录留痕。

### 3.12 综合数据统计
- 实验室使用率、设备使用率等可视化统计；
- 设备状态分布、借用记录状态、借用申请状态、报修工单状态实时图表（ECharts 可视化）。

## 4. 技术架构

| 层次 | 技术选型 |
| --- | --- |
| 表现层 | HTML5、CSS3、JavaScript、ECharts 5、SVG 图标 |
| 接口层 | RESTful API（JSON 数据交互） |
| 业务层 | Spring Boot 2.7、Spring MVC |
| 数据层 | MyBatis、MySQL 8 |
| 安全层 | JWT（jjwt）、BCrypt 密码加密、拦截器鉴权 |

## 5. 系统架构图（逻辑）

```
浏览器（前端页面）
   │  HTTP/JSON（JWT Bearer Token）
   ▼
Spring Boot 后端
   ├─ AuthInterceptor（登录鉴权 + @RequireRole 权限校验）
   ├─ Controller 层（接口）
   ├─ Service 层（业务逻辑，事务控制）
   └─ Mapper 层（MyBatis 数据访问）
           │
           ▼
       MySQL 数据库
```

## 6. 数据库设计

共 17 张数据表：

| 表名 | 说明 |
| --- | --- |
| role | 角色表（角色编码、角色名称） |
| user | 用户表（账号、密码、姓名、手机、邮箱、角色、状态、诚信分） |
| device_category | 设备分类表 |
| device | 设备表（编号、名称、分类、型号、位置、状态等） |
| borrow_apply | 借用申请表（申请理由、预计归还、审批状态等） |
| borrow_record | 借用记录表（借用/归还时间、状态等） |
| repair_record | 报修记录表（故障描述、维修状态、处理结果等） |
| notice | 公告表（标题、内容、发布时间、状态等） |
| lab_room | 实验室信息表 |
| lab_reservation | 实验室预约表 |
| device_reservation | 设备预约表 |
| device_purchase | 设备采购表 |
| consumable | 实验室耗材表 |
| hazardous_chemical | 危化品标准库表 |
| waste | 废弃物管理表 |
| credit_log | 诚信评分变更记录表 |

## 7. 软件技术特点

1. **前后端分离**：前端静态页面与后端 RESTful API 解耦，便于独立部署与维护；
2. **无状态鉴权**：JWT Token 实现无状态登录，`@RequireRole` 注解实现细粒度权限控制；
3. **事务一致性**：借用审批、归还、报修处理、诚信扣分等核心业务采用事务控制，保证数据一致；
4. **诚信评分体系**：基于用户行为的信用评估，约束用户规范使用设备；
5. **科技可视化**：深色科技风界面 + ECharts 实时数据图表，数据一目了然；
6. **数据完整性**：外键约束 + 业务校验双重保障，防止脏数据。

## 8. 软件运行环境

| 项目 | 要求 |
| --- | --- |
| 服务器操作系统 | Windows 10/11、Linux |
| JDK | 8 及以上 |
| 数据库 | MySQL 8.0 |
| 浏览器 | Chrome、Edge 等现代浏览器 |
| 内存 | 建议 4GB 以上 |

## 9. 软件使用说明

详细操作步骤请参阅《用户使用文档.md》。核心流程如下：

1. 执行 `01_数据库脚本/lab_equipment_init.sql` 初始化数据库；
2. 启动后端服务（默认 8080 端口）；
3. 打开前端登录页，使用测试账号登录；
4. 按角色进入对应工作台完成设备借用、审批、归还、报修等操作。

## 10. 版权声明

本软件及其源程序、说明书均由项目方独立开发，未抄袭、未侵犯第三方知识产权。软件界面设计、代码实现、数据库结构均为原创。
