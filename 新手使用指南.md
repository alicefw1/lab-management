# 实验室综合管理系统：零基础使用指南

> 适用目录：`D:\web`  
> 适用人群：第一次接触 Java、MySQL 或前后端分离系统的学生、教师和管理员  
> 建议操作系统：Windows 10/11

---

## 1. 先弄明白：这个系统由什么组成

不要把“数据库、后端、前端”理解成三个不同系统。它们是同一个系统的三个组成部分，必须配合运行。

| 组成部分 | 通俗解释 | 本项目中的位置 | 默认端口 |
| --- | --- | --- | --- |
| 数据库 MySQL | 像档案室，保存用户、设备、预约、报告、成绩等数据 | MySQL 中的 `lab_equipment` 数据库 | 3306 |
| 后端 Spring Boot | 像办事窗口，验证账号权限、处理业务并读写数据库 | `D:\web\02_后端工程 lab-reserve-server（Spring Boot + MyBatis）` | 8080 |
| 前端网页 | 像用户看到的操作大厅，负责显示菜单、表格和按钮 | `D:\web\03_前端工程完整代码` | 5173 |

一次完整操作的过程是：

```text
浏览器页面 → 请求后端 → 后端检查身份和业务规则 → 读写 MySQL → 把结果返回页面
```

因此，正确启动顺序永远是：

```text
① MySQL → ② 后端 → ③ 前端 → ④ 浏览器登录
```

只打开 `login.html` 并不等于启动了系统。

---

## 2. 项目目录分别有什么用

```text
D:\web
├─ 01_数据库脚本
│  ├─ lab_equipment_init.sql          首次安装：创建数据库和基础数据
│  └─ 02_academic_asset_upgrade.sql   增加班级、排课、报告、成绩、维护等功能
├─ 02_后端工程 lab-reserve-server（Spring Boot + MyBatis）
│  ├─ src                              Java 后端源代码
│  ├─ pom.xml                          Maven 依赖配置
│  └─ target\lab-reserve-server-1.0.0.jar  已打包的后端程序
├─ 03_前端工程完整代码
│  ├─ login.html                       登录页
│  ├─ board_student.html               学生/教师基础工作台
│  ├─ board_lab.html                   实验员工作台
│  ├─ board_admin.html                 管理员工作台
│  └─ board_teaching.html              实验教学与资产运维中心
├─ deploy                              Docker、Nginx 部署模板
├─ README.md                           开发者项目说明
└─ 用户使用文档.md                      本指南
```

普通使用者主要接触数据库脚本、后端 JAR、前端目录和本指南，不需要随意修改 Java 源码。

---

## 3. 第一次运行前需要准备什么

### 3.1 必装软件

| 软件 | 推荐版本 | 检查命令/方法 |
| --- | --- | --- |
| MySQL Server | 8.0 | Windows 搜索“服务”，查看 `MySQL80` 是否存在 |
| JDK / Java | 8 或更高 | PowerShell 执行 `java -version` |
| Chrome 或 Edge | 较新版本 | 能正常打开网页即可 |
| Python | 3.x，可选 | 执行 `python --version` 或 `py --version` |

项目已经自带 Maven，路径为：

```text
D:\web\.tooling\maven\apache-maven-3.8.9\bin\mvn.cmd
```

所以即使系统提示“找不到 mvn”，也不需要重新安装 Maven。

### 3.2 如何打开 PowerShell

1. 按键盘 `Win + R`；
2. 输入 `powershell`；
3. 按回车；
4. 后文灰色代码块中的命令，可逐行复制到 PowerShell 执行。

### 3.3 确认 Java

```powershell
java -version
```

如果能看到类似 `java version "1.8..."` 或 `17...`，说明 Java 可用。若提示“无法识别 java”，需要安装 JDK 并配置 `PATH`。

---

## 4. 第一步：启动和初始化数据库

### 4.1 数据库到底要不要重新初始化

先根据实际情况选择：

- **第一次在新电脑安装**：依次运行两个 SQL 脚本。
- **数据库已有旧版系统数据**：先备份，只运行 `02_academic_asset_upgrade.sql`。
- **当前这台已经完成升级的电脑**：不要再运行初始化脚本，直接启动后端即可。

> **重要警告：** `lab_equipment_init.sql` 内含 `DROP DATABASE IF EXISTS lab_equipment`。运行它会删除原数据库并重新创建，原有业务数据会丢失。不要在已有正式数据的环境中重复执行。

### 4.2 启动 MySQL 服务

图形方式：

1. 按 `Win + R`，输入 `services.msc`；
2. 找到 `MySQL80`；
3. 状态应为“正在运行”；
4. 如果没有运行，右键选择“启动”。

也可以用管理员 PowerShell：

```powershell
Start-Service MySQL80
```

如果服务名称不是 `MySQL80`，请在“服务”窗口中查看真实名称。

### 4.3 推荐方法：用 MySQL Workbench 执行脚本

这是最适合新手的方法。

1. 打开 MySQL Workbench；
2. 点击本地 MySQL 连接；
3. 输入安装 MySQL 时设置的 root 密码；
4. 选择菜单 `File → Open SQL Script`；
5. 第一次安装先打开：

   ```text
   D:\web\01_数据库脚本\lab_equipment_init.sql
   ```

6. 点击闪电形“执行”按钮，等待执行结束；
7. 再打开并执行：

   ```text
   D:\web\01_数据库脚本\02_academic_asset_upgrade.sql
   ```

8. 左侧 `SCHEMAS` 点击刷新，应能看到 `lab_equipment`。

### 4.4 命令行方法

下面示例假设 MySQL 用户名是 `root`。把第一行的密码换成自己的密码：

```powershell
$env:MYSQL_PWD="你的MySQL密码"
Get-Content -Raw -Encoding UTF8 "D:\web\01_数据库脚本\lab_equipment_init.sql" |
  & "C:\Program Files\MySQL\MySQL Server 8.0\bin\mysql.exe" --default-character-set=utf8mb4 -uroot
Get-Content -Raw -Encoding UTF8 "D:\web\01_数据库脚本\02_academic_asset_upgrade.sql" |
  & "C:\Program Files\MySQL\MySQL Server 8.0\bin\mysql.exe" --default-character-set=utf8mb4 -uroot
Remove-Item Env:MYSQL_PWD
```

如果 MySQL 安装在其他位置，请修改 `mysql.exe` 的路径。

### 4.5 验证数据库是否成功

在 Workbench 新建查询并运行：

```sql
USE lab_equipment;
SHOW TABLES;
SELECT username, real_name FROM user LIMIT 5;
```

正确现象：

- `SHOW TABLES` 返回多张表，而不是报“数据库不存在”；
- 用户查询能看到 `admin`、`student001` 等账号；
- 升级后的库还应包含 `lab_class`、`experiment_schedule`、`experiment_report`、`supplier` 和 `maintenance_plan`。

---

## 5. 第二步：启动后端

后端负责登录验证、权限判断和数据库读写。后端窗口关闭后，网页仍可能显示，但登录和数据操作会失败。

### 5.1 最简单方法：直接运行已经打包好的 JAR

新开一个 PowerShell 窗口，执行：

```powershell
$env:DB_USERNAME="root"
$env:DB_PASSWORD="你的MySQL密码"
java -jar "D:\web\02_后端工程 lab-reserve-server（Spring Boot + MyBatis）\target\lab-reserve-server-1.0.0.jar"
```

如果你的数据库不在本机或端口不是 3306，还要设置：

```powershell
$env:DB_URL="jdbc:mysql://数据库地址:3306/lab_equipment?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true"
```

### 5.2 什么样算启动成功

不要只看窗口有没有文字。控制台必须出现类似：

```text
Tomcat started on port(s): 8080
Started LabReserveApplication
```

看到这两句后，保留这个 PowerShell 窗口，不要关闭。

再开一个 PowerShell 验证端口：

```powershell
Test-NetConnection localhost -Port 8080
```

看到 `TcpTestSucceeded : True` 表示后端正在运行。

### 5.3 源码修改后如何重新打包

普通使用者不需要每次打包。只有修改 Java 源码后才执行：

```powershell
cd "D:\web\02_后端工程 lab-reserve-server（Spring Boot + MyBatis）"
& "D:\web\.tooling\maven\apache-maven-3.8.9\bin\mvn.cmd" clean package
```

构建成功后，会重新生成：

```text
target\lab-reserve-server-1.0.0.jar
```

然后按 5.1 节重新启动。

### 5.4 用 IDEA 启动（开发人员）

1. IDEA 选择 `File → Open`；
2. 打开后端工程目录；
3. 等待 Maven 依赖加载；
4. 打开 `LabReserveApplication.java`；
5. 点击绿色运行按钮；
6. 在运行配置中设置环境变量 `DB_USERNAME`、`DB_PASSWORD`；
7. 出现 `Started LabReserveApplication` 即成功。

---

## 6. 第三步：启动前端

不要直接双击 `login.html`。直接双击会使用 `file://` 地址，容易造成跨域、存储或接口访问问题。

### 6.1 使用 Python 启动

再新开一个 PowerShell 窗口：

```powershell
cd "D:\web\03_前端工程完整代码"
python -m http.server 5173
```

如果提示找不到 `python`，尝试：

```powershell
py -m http.server 5173
```

正确现象：

```text
Serving HTTP on ... port 5173
```

保留这个窗口，不要关闭。

### 6.2 打开系统

浏览器访问：

```text
http://localhost:5173/login.html
```

不要把 `8080` 和 `5173` 混淆：

- `5173` 是用户打开的网页；
- `8080` 是网页在后台访问的后端接口。

---

## 7. 第四步：登录系统

### 7.1 演示账号

| 账号 | 密码 | 角色 | 登录后的工作台 |
| --- | --- | --- | --- |
| `admin` | `123456` | 超级管理员 | 管理员工作台 |
| `lab` | `123456` | 实验员 | 实验员工作台 |
| `student` | `123456` | 学生 | 学生工作台 |
| `student001` | `123456` | 学生 | 学生工作台，含教学演示数据 |
| `teacher001` | `123456` | 教师 | 基础工作台 + 实验教学管理 |
| `lab001` | `123456` | 实验员 | 实验员工作台 |
| `admin001` | `123456` | 管理员 | 管理员工作台 |

演示账号只能用于本地测试。准备部署时必须修改默认密码。

### 7.2 注册账号

1. 登录页点击“立即注册”；
2. 填写账号、姓名、电话、邮箱和密码；
3. 点击注册；
4. 新注册账号默认为学生；
5. 其他角色需要管理员在“用户管理”中分配。

### 7.3 个人中心

登录后进入“个人中心”，可以：

- 查看账号、姓名和角色；
- 修改姓名、手机号和邮箱；
- 修改登录密码。

修改密码后请牢记新密码。管理员重置密码后，默认密码为 `123456`。

---

## 8. 学生怎样使用

### 8.1 借用设备

1. 进入“设备信息”；
2. 搜索设备名称或编号；
3. 找到状态为“可借”的设备；
4. 点击“申请借用”；
5. 填写借用原因和预计归还时间；
6. 提交后到“我的申请”查看审批状态；
7. 审批通过后，到“借用记录”查看借用状态。

诚信分低于 60、设备不可借或已有重复申请时，系统会拒绝提交。

### 8.2 预约实验室或设备

1. 进入“实验室预约”或“设备预约”；
2. 选择对象、日期和时间段；
3. 填写用途；
4. 提交后等待实验员或管理员审核；
5. 不再需要时，可在允许的状态下取消预约。

### 8.3 设备报修

1. 进入“设备报修”；
2. 点击“提交报修”；
3. 选择设备并写清故障现象；
4. 提交后查看“待处理、维修中、已完成”等状态。

### 8.4 实验安排、签到、报告和成绩

1. 在学生工作台右下角点击“实验教学管理”；
2. “实验安排”显示本班实验时间、实验室和教师；
3. 在允许签到的时间内点击“签到”；
4. 点击“写报告”，填写标题、实验过程、结果分析和结论；
5. 提交后进入“报告与成绩”；
6. 教师批改后可查看分数和教师评语。

签到开放范围为实验开始前 30 分钟到结束后 30 分钟。已批改的报告不能直接覆盖，需要联系教师处理。

### 8.5 在线咨询、留言和回复

1. 进入“反馈建议”；
2. 选择咨询、建议或投诉类型；
3. 填写标题和具体内容；
4. 提交后查看处理状态；
5. 管理员回复后，可在原记录中查看回复内容。

---

## 9. 教师怎样使用

教师登录后可以使用基础设备与预约功能。教学功能入口位于工作台右下角“实验教学管理”。

### 9.1 建立实验项目

1. 进入“实验项目”；
2. 点击“新建实验项目”；
3. 填写项目编号、名称、课程、学时、教学目标和安全事项；
4. 保存后记住项目 ID，排课时需要选择。

### 9.2 创建实验安排

1. 进入“实验安排”；
2. 点击“新建实验安排”；
3. 选择实验项目、班级、实验室、教师和起止时间；
4. 填写实验要求；
5. 如果教师或实验室在该时段已有安排，系统会提示冲突并禁止保存。

### 9.3 登记考勤

1. 在实验安排右侧点击“考勤”；
2. 查看班级学生名单；
3. 根据情况选择“出勤、缺勤、迟到”；
4. 学生自主签到的记录也会显示在名单中。

### 9.4 批改实验报告

1. 进入“报告与成绩”；
2. 找到待批改报告；
3. 点击“批改”；
4. 输入 0–100 分和教师评语；
5. 保存后学生立即可以查看结果。

教师只能管理自己的实验安排和自己授课班级的报告。

---

## 10. 实验员怎样使用

实验员主要处理实验室日常业务：

- “借用审批”：通过或拒绝学生借用申请；
- “借还登记”：办理设备归还；
- “逾期管理”：刷新逾期状态并处理逾期记录；
- “设备管理”：维护设备档案和分类；
- “报修工单”：登记维修中和维修完成；
- “实验室/设备预约审核”：审核预约；
- “采购管理”：查看采购申请；
- “实验教学管理”：协助排课和考勤；
- “设备维护”：建立维护计划、登记维护结果和费用。

办理归还后，设备状态会恢复为“可借”；完成维修后，应核对设备状态是否恢复正常。

---

## 11. 管理员怎样使用

管理员拥有全局管理权限，主要功能包括：

### 11.1 用户、学生和教师管理

1. 进入“用户管理”；
2. 可按姓名或账号搜索；
3. 新增用户时分配学生、教师、实验员或管理员角色；
4. 可编辑信息、冻结/解冻账号、重置密码；
5. 冻结账号后，该账号已有登录令牌也会立即失效。

学生管理和教师管理通过用户角色进行区分，不是两套重复账号表。

### 11.2 班级管理

1. 进入右下角“实验教学管理”；
2. 打开“班级管理”；
3. 创建班级并填写年级、专业、院系；
4. 点击“添加学生”，输入学生用户 ID 完成归班；
5. 学生归班后才能看到该班实验安排。

### 11.3 实验室、设备及类型

- “实验室类型”：维护实验室分类；
- “实验室管理”：维护名称、位置、容量、负责人和状态；
- “设备管理”：维护设备编号、名称、型号、分类、位置和状态；
- “供应商”：维护联系方式、经营范围和评分；
- “设备维护”：查看到期计划并登记维护记录；
- “设备报废”：审核报废申请。

### 11.4 其他管理

- 审核实验室预约、设备预约、借用和采购申请；
- 管理耗材、危化品和实验废弃物；
- 调整诚信分并查看变更记录；
- 发布公告；
- 回复在线咨询和留言；
- 查看环境监测、报警、仪器、知识库和操作日志；
- 通过数据看板查看设备、借用、报修和实验室使用情况。

---

## 12. 每天怎样启动和关闭

### 每天启动

1. 确认 `MySQL80` 服务运行；
2. 打开 PowerShell A，启动后端 JAR；
3. 等到出现 `Started LabReserveApplication`；
4. 打开 PowerShell B，在前端目录运行 `python -m http.server 5173`；
5. 浏览器访问 `http://localhost:5173/login.html`。

### 正常关闭

1. 在前端 PowerShell 窗口按 `Ctrl + C`；
2. 在后端 PowerShell 窗口按 `Ctrl + C`；
3. 等待程序退出；
4. MySQL 服务可保持运行，也可在测试结束后停止。

不要直接结束 MySQL 进程或强制关机，以免数据未及时写入。

---

## 13. 常见问题：按现象排查

### 13.1 网页打不开

检查前端窗口是否显示 `Serving HTTP ... port 5173`，并确认访问的是：

```text
http://localhost:5173/login.html
```

### 13.2 页面提示“无法连接后端”

```powershell
Test-NetConnection localhost -Port 8080
```

- `True`：后端端口可用，刷新页面并查看后端是否有错误；
- `False`：重新启动后端，等待 `Started LabReserveApplication`。

### 13.3 后端提示数据库连接失败

重点检查：

1. `MySQL80` 是否运行；
2. `DB_USERNAME`、`DB_PASSWORD` 是否正确；
3. 是否已经创建 `lab_equipment`；
4. MySQL 是否使用 3306 端口；
5. 错误信息中是否出现 `Access denied`。

`Access denied` 通常表示用户名或密码错误，不是 Java 代码错误。

### 13.4 8080 端口被占用

```powershell
Get-NetTCPConnection -LocalPort 8080 -State Listen
```

关闭重复启动的旧后端。不要同时运行 IDEA 后端和 JAR 后端。

### 13.5 5173 端口被占用

说明已经有前端服务器，先尝试直接访问登录地址。确需重新启动时，找到原前端窗口并按 `Ctrl + C`。

### 13.6 登录失败

- “账号不存在”：数据库可能未初始化或账号输入错误；
- “密码错误”：核对密码，注意输入法和大小写；
- “账号已冻结”：联系管理员解冻；
- 所有账号都失败：检查后端和数据库是否启动。

### 13.7 Maven 下载依赖失败

确认电脑能访问互联网，然后重新运行 Maven。第一次构建需要下载依赖，耗时较长；后续会使用本机缓存。

### 13.8 中文乱码

执行 SQL 时选择 UTF-8，命令行增加：

```text
--default-character-set=utf8mb4
```

不要把 SQL 文件另存为 ANSI 或 GBK。

### 13.9 忘记数据库密码

这是 MySQL 安装时设置的密码，不等于系统登录密码。需要按照 MySQL 官方流程重置，不能通过系统“重置用户密码”解决。

---

## 14. 数据备份与安全提醒

### 14.1 修改数据库前先备份

```powershell
$env:MYSQL_PWD="你的MySQL密码"
& "C:\Program Files\MySQL\MySQL Server 8.0\bin\mysqldump.exe" `
  --single-transaction -uroot `
  --result-file="D:\web\lab_equipment_backup.sql" lab_equipment
Remove-Item Env:MYSQL_PWD
```

确认备份文件不是 0 KB 后，再执行升级或修改。

### 14.2 正式部署前必须处理

本地能运行不等于可以直接投入学校生产使用。正式上线前至少需要：

- 修改所有演示账号默认密码；
- 使用高强度 `JWT_SECRET`；
- 配置 HTTPS、正式域名和服务器防火墙；
- 接入学校统一身份认证和真实组织数据；
- 制定数据库自动备份与恢复方案；
- 完成隐私告知、日志审计、漏洞扫描和压力测试；
- 由学校信息化及网络安全负责人验收。

生产部署模板位于 `D:\web\deploy`，但不建议零基础用户未经审核直接部署到公网。

---

## 15. 最短启动清单

如果数据库已经初始化，只需要记住下面四步：

```text
1. 启动 MySQL80
2. java -jar 后端 JAR，等到 Started LabReserveApplication
3. 在前端目录运行 python -m http.server 5173
4. 打开 http://localhost:5173/login.html
```

遇到问题时先判断是哪一层：

```text
网页打不开 → 前端问题
网页能开但无法登录 → 后端或数据库问题
后端提示 Access denied → MySQL 账号密码问题
登录成功但某按钮不能用 → 角色权限或业务状态问题
```
