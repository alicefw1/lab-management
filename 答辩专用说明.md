# 实验室设备预约管理系统答辩专用说明

## 一、项目基本信息

**项目名称：** 实验室设备预约管理系统

**项目定位：** 面向高校实验室的设备预约、审核和使用管理平台。

**项目目标：** 将传统的线下登记、人工沟通和表格统计，改造成可查询、可预约、可审核、可统计的 Web 管理系统。

**技术架构：**

```text
浏览器前端 HTML/CSS/JavaScript
            |
         REST API
            |
Spring Boot + Spring MVC + MyBatis
            |
          MySQL
```

## 二、项目功能亮点

### 1. 角色化业务流程

系统根据用户角色进行页面分流：

- 学生：浏览设备、提交预约、查看个人预约、查看公告、查看个人资料。
- 实验室管理员：管理设备、审核预约、发布公告、查看统计。
- 超级管理员：拥有管理端全部功能，可作为系统演示账号。

### 2. 预约时间冲突校验

系统使用时间区间相交算法判断设备是否被占用：

```text
新预约开始时间 < 已有预约结束时间
并且
新预约结束时间 > 已有预约开始时间
```

只有两个条件同时满足，才认为时间冲突。这样可以正确处理完全重叠、部分重叠和包含关系，并允许前一预约结束后紧接着开始下一预约。

此外，提交预约时校验一次，管理员审核通过时再校验一次，降低并发或先后审核导致的重复占用风险。

### 3. 设备全生命周期管理

管理端支持设备新增、编辑、删除、状态维护和分类筛选。设备状态包括：

- 空闲
- 已预约
- 使用中
- 维修中
- 停用

### 4. 公告发布和置顶

管理员可以发布、修改、删除公告，并设置置顶。学生端按置顶和发布时间查看公告，保证重要通知优先展示。

### 5. 真实数据统计

统计页不使用固定演示数字，而是从数据库读取预约记录，动态计算：

- 预约总数
- 待审核数量
- 已通过数量
- 已驳回数量
- 设备总数
- 公告数量
- 预约状态饼图
- 近 7 天预约趋势图

### 6. 统一接口返回和异常处理

后端统一使用 `Result<T>` 封装接口返回值，返回码、提示信息和数据结构保持一致。参数错误通过全局异常处理器转换为 JSON，前端能够直接提示用户，而不是显示难以理解的服务器错误页面。

## 三、页面结构

### 1. 登录页

文件：`03_前端工程完整代码/login.html`

功能：

- 输入账号和密码。
- 选择学生、管理员或超级管理员身份。
- 登录成功后保存用户信息。
- 根据角色自动跳转对应首页。

### 2. 学生端

| 页面 | 文件 | 主要功能 |
| --- | --- | --- |
| 首页 | `student/index.html` | 查看个人预约数量、待审核数量和违规次数 |
| 设备预约 | `student/device.html` | 浏览设备，筛选可用设备，提交预约 |
| 我的预约 | `student/my-reservation.html` | 查看自己的预约时间、用途和状态 |
| 公告通知 | `student/notice.html` | 查看管理员发布的公告 |
| 个人中心 | `student/profile.html` | 查看姓名、账号、学院、电话和违规次数 |

### 3. 管理端

| 页面 | 文件 | 主要功能 |
| --- | --- | --- |
| 管理首页 | `admin/index.html` | 查看设备、预约、公告数量和功能入口 |
| 设备管理 | `admin/device-manage.html` | 设备增删改查、分类筛选、状态筛选 |
| 预约审核 | `admin/reservation-audit.html` | 查看全部预约，通过、驳回、取消预约 |
| 公告管理 | `admin/notice-manage.html` | 公告增删改查、置顶设置 |
| 数据统计 | `admin/statistics.html` | 真实统计卡片、饼图和趋势图 |

## 四、数据库设计

系统数据库名称为 `lab_reserve`，核心数据表如下。

### 1. 用户表 user

保存登录账号、密码、姓名、角色、学院、联系电话、违规次数和账号状态。

关键字段：

- `id`：用户主键
- `username`：登录账号，唯一
- `password`：MD5 密码
- `role`：1 学生，2 实验室管理员，3 超级管理员
- `status`：1 正常，0 冻结

### 2. 设备分类表 device_category

保存设备分类名称和显示排序，与设备表建立一对多关系。

### 3. 设备表 device

保存设备编号、名称、分类、规格、厂家、位置、描述、状态和图片地址。

关键关系：

```text
device.category_id -> device_category.id
```

### 4. 预约表 reservation

保存申请人、设备、预约开始时间、结束时间、用途、人数、审核状态和原因。

关键关系：

```text
reservation.user_id   -> user.id
reservation.device_id -> device.id
```

状态含义：

- `1`：待审核
- `2`：已通过
- `3`：已驳回
- `4`：使用中
- `5`：已完成
- `6`：已取消

### 5. 公告表 notice

保存公告标题、内容、发布人、置顶状态和创建时间。

关键关系：

```text
notice.publisher_id -> user.id
```

### 6. 违规记录表 violation

保存用户违规类型、违规说明和关联预约记录，为后续信用管理功能预留数据结构。

## 五、后端分层设计

### Controller 层

负责接收 HTTP 请求、读取参数、调用 Service 并返回统一结果。

主要控制器：

- `UserController`：登录和用户信息
- `DeviceController`：设备管理
- `DeviceCategoryController`：设备分类查询
- `ReservationController`：预约提交和审核
- `NoticeController`：公告管理
- `StatisticsController`：统计数据

### Service 层

负责业务规则和数据校验：

- 预约时间冲突算法
- 预约时间合法性校验
- 审核状态校验
- 设备和公告必填字段校验
- 审核通过前的二次冲突校验

### Mapper 层

负责通过 MyBatis 执行数据库查询、插入、修改和删除操作。

### 配置层

- `CorsConfig`：允许前端静态页面访问后端接口。
- `GlobalExceptionHandler`：统一处理参数异常和服务器异常。
- `application.yml`：配置端口、数据库和 MyBatis 映射位置。

## 六、答辩演示流程

### 第一部分：系统登录

1. 打开登录页。
2. 输入 `admin / 123456`。
3. 选择“超级管理员”。
4. 登录后进入管理端首页。
5. 说明系统根据角色进行页面分流。

### 第二部分：展示管理首页和统计

1. 在管理首页展示设备总数、预约总数和公告数量。
2. 进入数据统计页面。
3. 展示真实数据卡片。
4. 说明饼图根据预约状态动态生成，趋势图根据最近 7 天预约创建时间计算。

### 第三部分：演示设备管理

1. 进入设备管理。
2. 展示初始化的示波器、万用表、实验电脑和离心机。
3. 使用状态筛选和分类筛选。
4. 新增一台设备，填写编号、名称、分类、位置和状态。
5. 编辑设备名称或状态。
6. 删除刚新增的设备。

### 第四部分：演示公告管理

1. 进入公告管理。
2. 发布一条公告。
3. 勾选置顶并保存。
4. 编辑公告内容。
5. 删除公告。
6. 说明学生端会读取同一接口，看到最新公告。

### 第五部分：演示学生预约

1. 退出管理员账号。
2. 使用学生账号登录。
3. 进入设备预约页面。
4. 选择空闲设备。
5. 选择未来日期和时间，填写用途并提交。
6. 进入我的预约页面，查看待审核记录。

### 第六部分：演示预约审核

1. 切换回管理员账号。
2. 进入预约审核页面。
3. 找到刚刚提交的预约。
4. 点击通过，展示状态变化。
5. 再演示驳回操作，填写驳回原因。
6. 说明审核通过时会再次检查同一设备的时间冲突。

## 七、老师可能提问及回答

### 问题 1：为什么选择 Spring Boot？

回答：Spring Boot 可以快速搭建独立运行的 Web 应用，自动配置常用组件，减少 XML 和服务器配置工作，适合课程设计快速实现完整业务。项目使用 Spring MVC 接口接收请求，使用 MyBatis 操作 MySQL，分层结构清晰，便于维护。

### 问题 2：如何防止重复预约？

回答：提交预约时查询同一设备的待审核、已通过和使用中的记录，通过时间区间相交算法判断冲突。管理员审核通过前会再次检查，避免预约提交后产生新的冲突。

### 问题 3：为什么前端使用静态 HTML？

回答：本项目重点是 Web 系统的业务流程和前后端接口设计。静态 HTML 配合原生 JavaScript、Bootstrap 和 ECharts，部署简单、依赖少，同时可以清楚展示 REST 接口调用和页面交互过程。

### 问题 4：系统如何处理异常输入？

回答：Service 层会校验预约时间、用途、设备编号、设备名称、设备分类、公告标题和公告内容。非法参数通过全局异常处理器统一返回 JSON，前端根据提示信息展示给用户。

### 问题 5：系统还有哪些扩展方向？

回答：可以继续增加 JWT 登录认证、管理员权限拦截、预约取消、设备图片上传、公告分页、违规自动计分、邮件或短信提醒、操作日志和预约日历视图。

## 八、项目优势总结

本项目具备以下适合作为优秀课程设计展示的特点：

1. 业务场景明确，符合实验室日常管理需求。
2. 具备学生端和管理端双角色使用流程。
3. 包含数据库设计、REST API、业务校验和前端交互。
4. 预约冲突算法具有实际业务价值。
5. 管理端三个核心模块已经形成真实 CRUD 闭环。
6. 统计图表使用后端真实数据，不是固定假数据。
7. 工程目录、Maven 配置和使用文档完整。
8. 具备继续扩展权限、日志和通知功能的基础。
